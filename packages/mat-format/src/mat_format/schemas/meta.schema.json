{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/GameOfPods/MAT/master/packages/mat-format/src/mat_format/schemas/meta.schema.json",
  "$comment": "MAT result format 2, describes meta.json. Generated from mat_format/models.py, don't edit by hand.",
  "$defs": {
    "Input": {
      "description": "The media file that was processed.",
      "properties": {
        "name": {
          "description": "File name without folders.",
          "title": "Name",
          "type": "string"
        },
        "path": {
          "description": "Absolute path on the machine that ran MAT.",
          "title": "Path",
          "type": "string"
        },
        "sha1": {
          "description": "SHA-1 of the file content, hex encoded.",
          "title": "Sha1",
          "type": "string"
        }
      },
      "required": [
        "name",
        "path",
        "sha1"
      ],
      "title": "Input",
      "type": "object"
    }
  },
  "description": "Content of meta.json in the root of every result.",
  "properties": {
    "format": {
      "const": 2,
      "default": 2,
      "description": "Result format version.",
      "title": "Format",
      "type": "integer"
    },
    "mat_version": {
      "description": "Version of MAT that wrote the result.",
      "title": "Mat Version",
      "type": "string"
    },
    "created": {
      "description": "Local time the result was written, ISO 8601 without time zone.",
      "title": "Created",
      "type": "string"
    },
    "input": {
      "$ref": "#/$defs/Input"
    },
    "pipelines": {
      "description": "Pipelines that ran, each has a folder of the same name: \"podcast\", \"book\".",
      "items": {
        "type": "string"
      },
      "title": "Pipelines",
      "type": "array"
    }
  },
  "required": [
    "mat_version",
    "created",
    "input",
    "pipelines"
  ],
  "title": "Meta",
  "type": "object"
}
